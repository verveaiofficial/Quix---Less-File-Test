import React, { useEffect, useRef, useState } from "react";
import { create } from "zustand";
import { MODELS, CHAT_MODELS, LIMITS, IMAGINE_URL, APP_VERSION, THINK_SEP, rid, ChatMessage, SourceItem, AttachmentMeta, useAuthStore, useUIStore, useChatStore, useProfileStore, useMemoryStore, useUsageStore, supabase, createChat, insertMessage, fetchChats, fetchMessages, renameChat, deleteChat, askGeminiStream, abortGemini, buildPrompt, runDailyMemorySync, stripObs, extractObs, saveObservation } from "./core";
import { CanvasPanel, FileCard, InlineCodeBlock, useCanvasStore, extractFiles } from "./canvas";

/* crash guard: shows the error instead of a blank screen */
class ErrorBoundary extends React.Component<{ children: any }, { err: string }> {
  constructor(p: any) { super(p); this.state = { err: "" }; }
  static getDerivedStateFromError(e: any) { return { err: String((e && e.message) || e) }; }
  render() {
    if (this.state.err) return (<div style={{ padding: 24, color: "#ff8080", fontFamily: "ui-monospace,monospace", fontSize: 12, whiteSpace: "pre-wrap", background: "#000", minHeight: "100dvh" }}>{"QUIX hit an error:\n\n" + this.state.err + "\n\nScreenshot this and send it to your dev."}</div>);
    return this.props.children;
  }
}

const CHAIN: Record<string, string[]> = { flash: ["flash", "lite"], lite: ["lite", "flash"], thinking: ["thinking"], deepthink: ["deepthink", "thinking"], coder: ["coder", "flash", "lite"] };
const PIN_KEY = "quix_pinned_v1";
const usePinStore = create<any>((set, get) => ({ pinned: (() => { try { return JSON.parse(localStorage.getItem(PIN_KEY) || "[]"); } catch { return []; } })(), toggle: (id: string) => { if (!id) return; const cur: string[] = get().pinned; const next = cur.includes(id) ? cur.filter((x) => x !== id) : [...cur, id]; try { localStorage.setItem(PIN_KEY, JSON.stringify(next)); } catch {} set({ pinned: next }); } }));
const useImagineStore = create<any>((set) => ({ nonce: 0, bump: () => set((s: any) => ({ nonce: s.nonce + 1 })) }));
function shimmer(e: any) { const el = e.currentTarget as HTMLElement; el.classList.remove("shimmer"); void el.offsetWidth; el.classList.add("shimmer"); setTimeout(() => el.classList.remove("shimmer"), 500); }
function shimmerThen(e: any, fn: () => void) { shimmer(e); setTimeout(fn, 450); }

const globalCSS = `:root{color-scheme:dark}html,body{background:#000}.shimmer-btn{position:relative;overflow:hidden}.shimmer-btn::after{content:'';position:absolute;top:0;bottom:0;left:0;width:40%;background:linear-gradient(90deg,transparent,rgba(255,255,255,.22),transparent);transform:translateX(-100%);pointer-events:none}.shimmer-btn.shimmer::after{animation:shimmerSwipe .45s ease}@keyframes shimmerSwipe{from{transform:translateX(-100%)}to{transform:translateX(350%)}}.md-wrap{display:flex;flex-direction:column;gap:10px}.md-text{font-size:15px;line-height:1.65;color:#e5e7eb;word-wrap:break-word}.md-text h3,.md-text h4{color:#fff;margin:8px 0 4px}.md-text ul,.md-text ol{padding-left:20px;margin:4px 0}.md-text a{color:#7ab8ff;text-decoration:underline}.md-code{background:rgba(255,255,255,.08);border-radius:6px;padding:1px 6px;font-size:13px;font-family:ui-monospace,monospace}.md-gap{height:8px}`;

function escapeHtml(s: string) { return s.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;"); }
function inlineMd(s: string) { return s.replace(/`([^`]+)`/g, '<code class="md-code">$1</code>').replace(/\*\*([^*]+)\*\*/g, "<strong>$1</strong>").replace(/\*([^*]+)\*/g, "<em>$1</em>").replace(/\[([^\]]+)\]\((https?:[^)]+)\)/g, '<a href="$2" target="_blank" rel="noreferrer">$1</a>'); }
function mdToHtml(src: string): string { const lines = escapeHtml(src).split("\n"); const out: string[] = []; let inUl = false, inOl = false; const close = () => { if (inUl) out.push("</ul>"); if (inOl) out.push("</ol>"); inUl = false; inOl = false; }; for (const line of lines) { const t = line.trim(); if (!t) { close(); out.push('<div class="md-gap"></div>'); continue; } if (/^###\s/.test(t)) { close(); out.push(`<h4>${inlineMd(t.slice(4))}</h4>`); continue; } if (/^##\s/.test(t)) { close(); out.push(`<h3>${inlineMd(t.slice(3))}</h3>`); continue; } if (/^#\s/.test(t)) { close(); out.push(`<h3>${inlineMd(t.slice(2))}</h3>`); continue; } if (/^[-*]\s/.test(t)) { if (!inUl) { close(); out.push("<ul>"); inUl = true; } out.push(`<li>${inlineMd(t.slice(2))}</li>`); continue; } if (/^\d+[.)]\s/.test(t)) { if (!inOl) { close(); out.push("<ol>"); inOl = true; } out.push(`<li>${inlineMd(t.replace(/^\d+[.)]\s/, ""))}</li>`); continue; } close(); out.push(`<div>${inlineMd(t)}</div>`); } close(); return out.join(""); }
function MarkdownText({ text, mid, canvas }: { text: string; mid: string; canvas: boolean }) { const parts = text.split(/```/); return (<div className="md-wrap">{parts.map((part, i) => { if (i % 2 === 1) { const nl = part.indexOf("\n"); const lang = nl > -1 ? part.slice(0, nl).trim() : ""; const code = nl > -1 ? part.slice(nl + 1) : part; return canvas ? <FileCard key={i} lang={lang || "txt"} code={code} fid={`${mid}-${i}`} num={(i + 1) / 2} /> : <InlineCodeBlock key={i} lang={lang || "code"} code={code} />; } if (!part.trim()) return null; return <div key={i} className="md-text" dangerouslySetInnerHTML={{ __html: mdToHtml(part) }} />; })}</div>); }

const ORB_COLORS = ["#00f2ff", "#ff00c8", "#39ff14", "#6e5fff", "#ff0088", "#ffff00", "#00ffdd", "#a855f7"];
function BubbleIndicator({ size = 26, dimmed = false }: { size?: number; dimmed?: boolean }) { const ref = useRef<HTMLCanvasElement>(null); useEffect(() => { const canvas = ref.current; if (!canvas) return; const ctx = canvas.getContext("2d"); if (!ctx) return; const W = size, H = size, R = size / 2, cx = R, cy = R; const blobs = ORB_COLORS.map((color, i) => ({ fx: 0.71 + i * 0.09, fy: 1.13 - i * 0.05, phase: i * 0.9, amp: 0.5, r: R * 0.75, color })); let t = 0, last = performance.now(), id = 0; const draw = (now: number) => { const dt = now - last; last = now; const s = (Math.sin(((now % 5000) / 5000) * Math.PI * 2 - Math.PI / 2) + 1) / 2; t += (0.12 + (2.2 - 0.12) * s) * dt * 0.001; ctx.clearRect(0, 0, W, H); ctx.save(); ctx.beginPath(); ctx.arc(cx, cy, R, 0, Math.PI * 2); ctx.clip(); const bg = ctx.createRadialGradient(cx * 0.84, cy * 0.76, 1, cx, cy, R); bg.addColorStop(0, "#1a1a2e"); bg.addColorStop(0.3, "#0f1f3d"); bg.addColorStop(0.55, "#2a1b4d"); bg.addColorStop(0.8, "#3d1f4d"); bg.addColorStop(1, "#0f2a3d"); ctx.fillStyle = bg; ctx.fillRect(0, 0, W, H); ctx.globalCompositeOperation = "screen"; blobs.forEach((b) => { const bx = cx + Math.sin(b.fx * t + b.phase) * R * b.amp; const by = cy + Math.cos(b.fy * t + b.phase * 1.4) * R * b.amp; const br = b.r * (1 + 0.08 * Math.sin(b.fx * t * 2.3 + b.phase)); const g = ctx.createRadialGradient(bx, by, 0, bx, by, br); g.addColorStop(0, b.color + "cc"); g.addColorStop(0.35, b.color + "88"); g.addColorStop(0.7, b.color + "33"); g.addColorStop(1, b.color + "00"); ctx.beginPath(); ctx.arc(bx, by, br, 0, Math.PI * 2); ctx.fillStyle = g; ctx.fill(); }); ctx.restore(); id = requestAnimationFrame(draw); }; id = requestAnimationFrame(draw); return () => cancelAnimationFrame(id); }, [size]); return (<canvas ref={ref} width={size} height={size} style={{ borderRadius: "50%", display: "block", flexShrink: 0, opacity: dimmed ? 0.35 : 1, filter: dimmed ? "grayscale(100%)" : "none", transition: "opacity .5s ease, filter .5s ease" }} />); }

function faviconUrl(uri: string): string | null { try { return "https://www.google.com/s2/favicons?domain=" + new URL(uri).hostname + "&sz=32"; } catch { return null; } }
const qtsCSS = `.qts-status{margin-bottom:12px;width:100%}.qts-head{display:flex;align-items:center;gap:12px;padding:6px 0;user-select:none}.qts-title-row{display:flex;align-items:baseline;gap:6px;flex:1;min-width:0}.qts-title{font-size:14.5px;font-weight:600;color:#6b6f76;white-space:nowrap;line-height:1.2;transition:color .3s ease}.qts-status.active .qts-title{color:#f5f5f5}.qts-status.done .qts-title{color:#8a8f96}.qts-meta{font-size:13px;color:#55585e;line-height:1.2}.qts-toggle{display:none;align-items:center;justify-content:center;width:16px;height:16px;color:#8a8f96;cursor:pointer;background:none;border:none;padding:0;transition:transform .3s ease}.qts-status.done .qts-toggle{display:inline-flex}.qts-toggle.open{transform:rotate(180deg)}.qts-toggle svg{stroke:currentColor}.qts-reason{margin:2px 0 6px 12px;padding-left:14px;border-left:1px solid #2a2a2e;display:flex;flex-direction:column;gap:10px}.qts-meta-row{display:flex;align-items:center;gap:8px;font-size:13px;color:rgba(255,255,255,.55)}.qts-reason-icon{display:inline-flex;width:12px;height:12px;color:#8a8f96;flex-shrink:0}.qts-reason-icon svg{display:block;stroke:currentColor}.qts-favstack{display:inline-flex;align-items:center}.qts-favstack img{width:16px;height:16px;border-radius:50%;margin-left:-5px;border:1px solid rgba(0,0,0,.8);background:#1c1c22}.qts-favstack img:first-child{margin-left:0}.qts-thought{display:flex;gap:8px;font-size:12.5px;line-height:1.65;color:rgba(255,255,255,.42)}.qts-bullet{color:rgba(255,255,255,.35);flex-shrink:0}`;
const searchIconSvg = '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>';
const pageIconSvg = '<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M14 2H6a2 2 0 00-2 2v16a2 2 0 002 2h12a2 2 0 002-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/></svg>';
function ThinkingStatus({ done, sources, thoughts, thinkTime }: { done: boolean; sources?: SourceItem[]; thoughts?: string; thinkTime?: number }) { const [elapsed, setElapsed] = useState(0); const [expanded, setExpanded] = useState(true); useEffect(() => { if (done) return; const t = setInterval(() => setElapsed((p) => p + 1), 1000); return () => clearInterval(t); }, [done]); const finalTime = thinkTime != null ? thinkTime : elapsed; const thoughtParas = (thoughts || "").split(/\n+/).map((t) => t.trim()).filter(Boolean); const found = sources?.length ?? 0; const read = Math.min(4, found); const favs = (sources || []).slice(0, 4).map((s) => faviconUrl(s.uri)).filter(Boolean) as string[]; const hasReason = thoughtParas.length > 0 || found > 0; return (<div className={`qts-status visible ${done ? "done" : "active"}`}><style>{qtsCSS}</style><div className="qts-head"><BubbleIndicator size={26} dimmed={done} /><div className="qts-title-row"><span className="qts-title">{done ? (finalTime > 0 ? `Thought for ${finalTime}s` : "Thought") : "Thinking"}</span>{!done && <span className="qts-meta">{elapsed}s</span>}{done && hasReason && (<button className={`qts-toggle ${expanded ? "open" : ""}`} onClick={() => setExpanded((p) => !p)}><svg width="12" height="12" viewBox="0 0 24 24" fill="none" strokeWidth={2.5} strokeLinecap="round" strokeLinejoin="round"><polyline points="6 9 12 15 18 9" /></svg></button>)}</div></div>{(!done || expanded) && hasReason && (<div className="qts-reason">{found > 0 && (<div className="qts-meta-row"><span className="qts-reason-icon" dangerouslySetInnerHTML={{ __html: searchIconSvg }} /><span>Found {found} web pages</span>{favs.length > 0 && <span className="qts-favstack">{favs.map((f, i) => <img key={i} src={f} alt="" />)}</span>}</div>)}{read > 0 && (<div className="qts-meta-row"><span className="qts-reason-icon" dangerouslySetInnerHTML={{ __html: pageIconSvg }} /><span>Read {read} pages</span>{favs.length > 0 && <span className="qts-favstack">{favs.map((f, i) => <img key={i} src={f} alt="" />)}</span>}</div>)}{thoughtParas.map((t, i) => (<div className="qts-thought" key={i}><span className="qts-bullet">•</span><span>{t}</span></div>))}</div>)}</div>); }

const umCSS = `.um-wrap{align-self:flex-end;max-width:85%;margin-left:auto;margin-bottom:24px;display:flex;flex-direction:column;align-items:flex-end}.um-wrap .message-user{margin-bottom:0;margin-left:0;max-width:100%;align-self:auto}.um-actions{display:flex;gap:6px;margin-top:6px;opacity:.5;transition:opacity .2s ease}.um-actions:hover{opacity:1}.um-actions button{background:none;border:none;color:rgba(255,255,255,.7);cursor:pointer;padding:3px 5px;border-radius:6px;display:flex;align-items:center}.um-actions button:hover{background:rgba(255,255,255,.08);color:#fff}.um-actions svg{stroke:currentColor}`;
function UserMessage({ content, attachments, mid }: { content: string; attachments?: AttachmentMeta[]; mid: string }) { return (<div className="um-wrap" data-mid={mid}><style>{umCSS}</style><div className="message-user">{attachments && attachments.length > 0 && (<div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginBottom: content ? 10 : 0 }}>{attachments.map((a, i) => (<div key={i} style={{ display: "flex", alignItems: "center", gap: 6, background: "rgba(255,255,255,0.08)", border: "1px solid rgba(255,255,255,0.12)", borderRadius: 8, padding: "4px 8px", fontSize: 11, color: "rgba(255,255,255,0.7)", maxWidth: 160 }}>{a.kind === "image" && a.previewUrl ? (<img src={a.previewUrl} alt={a.name} style={{ width: 26, height: 26, borderRadius: 6, objectFit: "cover" }} />) : null}<span style={{ overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{a.name}</span></div>))}</div>)}{content}</div><div className="um-actions"><button onClick={() => safeCopy(content)}><svg width="12" height="12" viewBox="0 0 24 24" fill="none" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><rect x="9" y="9" width="13" height="13" rx="2" /><path d="M5 15H4a2 2 0 01-2-2V4a2 2 0 012-2h9a2 2 0 012 2v1" /></svg></button></div></div>); }

const amCSS = `.msg-actions{display:flex;gap:4px;margin-top:8px;opacity:.55;transition:opacity .2s ease}.msg-actions:hover{opacity:1}.msg-actions button{background:none;border:none;cursor:pointer;padding:4px 7px;border-radius:8px;color:rgba(255,255,255,.6);display:flex;align-items:center}.msg-actions button:hover{background:rgba(255,255,255,.06);color:#fff}.msg-actions button.active{background:rgba(255,255,255,.1);color:#fff}.msg-actions svg{stroke:currentColor}`;
function AiMessage({ message, mid }: { message: ChatMessage & { thinkTime?: number }; mid: string }) { const { updateMessage } = useChatStore(); const canvasOn = useCanvasStore((s) => s.on); const hasCode = (message.content || "").includes("```"); const shouldStream = message.status === "streaming"; const shown = useStreamText(message.content || "", shouldStream, 10); const isDone = message.status === "done" || message.status === "error"; const isThinkingModel = message.model === "thinking" || message.model === "deepthink"; useEffect(() => { if (message.status !== "streaming") return; if (!message.doneStreaming) return; if (hasCode) { updateMessage(message.id, { status: "done" }); useChatStore.getState().setIsSending(false); const { currentChatId } = useChatStore.getState(); const session = useAuthStore.getState().session; if (currentChatId && session) insertMessage(currentChatId, { ...message, status: "done" }); return; } if (shown.length >= (message.content || "").length) { const t = setTimeout(() => { updateMessage(message.id, { status: "done" }); useChatStore.getState().setIsSending(false); const { currentChatId } = useChatStore.getState(); const session = useAuthStore.getState().session; if (currentChatId && session) insertMessage(currentChatId, { ...message, status: "done" }); }, 150); return () => clearTimeout(t); } }, [message, shown, hasCode, updateMessage]); useEffect(() => { const onStop = () => { const cur = useChatStore.getState().messages.find((m) => m.id === message.id); if (cur && cur.status === "streaming") { updateMessage(message.id, { content: shown, status: "done", doneStreaming: true }); useChatStore.getState().setIsSending(false); } }; window.addEventListener("quix-stop", onStop); return () => window.removeEventListener("quix-stop", onStop); }, [shown, message.id, updateMessage]); return (<div className="message-ai" data-mid={mid}><style>{amCSS}</style><div className="ai-content">{isThinkingModel ? (<ThinkingStatus done={message.status !== "thinking"} sources={message.sources} thoughts={message.thoughts} thinkTime={message.thinkTime} />) : (<BubbleIndicator dimmed={isDone} />)}<div style={{ width: "100%", maxWidth: 640 }}>{shouldStream && <MarkdownText text={shown} mid={mid} canvas={canvasOn} />}{message.status === "done" && <MarkdownText text={message.content || ""} mid={mid} canvas={canvasOn} />}{message.status === "error" && (<p className="typing-text" style={{ color: "#ff8080" }}>{message.content}</p>)}</div></div>{message.status === "done" && (<div className="msg-actions"><button className={message.feedback === "up" ? "active" : ""} onClick={() => updateMessage(message.id, { feedback: message.feedback === "up" ? undefined : "up" })}><svg width="13" height="13" viewBox="0 0 24 24" fill="none" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M7 10v12" /><path d="M15 5.88 14 10h5.83a2 2 0 0 1 1.92 2.56l-2.33 8A2 2 0 0 1 17.5 22H4a2 2 0 0 1-2-2v-8a2 2 0 0 1 2-2h2.76a2 2 0 0 0 1.79-1.11L12 2a3.13 3.13 0 0 1 3 3.88Z" /></svg></button><button className={message.feedback === "down" ? "active" : ""} onClick={() => updateMessage(message.id, { feedback: message.feedback === "down" ? undefined : "down" })}><svg width="13" height="13" viewBox="0 0 24 24" fill="none" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M17 14V2" /><path d="M9 18.12 10 14H4.17a2 2 0 0 1-1.92-2.56l2.33-8A2 2 0 0 1 6.5 2H20a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2h-2.76a2 2 0 0 0-1.79 1.11L12 22a3.13 3.13 0 0 1-3-3.88Z" /></svg></button><button onClick={() => safeCopy(message.content || "")}><svg width="13" height="13" viewBox="0 0 24 24" fill="none" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><rect x="9" y="9" width="13" height="13" rx="2" /><path d="M5 15H4a2 2 0 01-2-2V4a2 2 0 012-2h9a2 2 0 012 2v1" /></svg></button></div>)}</div>); }

const mlCSS = `.message-scroll{position:absolute;top:0;left:0;right:0;bottom:0;overflow-y:auto;z-index:1}.message-scroll::-webkit-scrollbar{width:0}.message-container{width:100%;max-width:650px;margin:0 auto;padding:24px 20px 140px;display:flex;flex-direction:column}.message-user{align-self:flex-end;max-width:85%;margin-left:auto;background-color:#1e1e20;color:#fff;padding:12px 16px;border-radius:18px 18px 4px 18px;font-size:15px;line-height:1.5;word-wrap:break-word;white-space:pre-wrap;margin-bottom:24px}.message-ai{align-self:flex-start;width:100%;font-size:16px;line-height:1.6;color:#e5e7eb;margin-bottom:24px}.ai-content{width:100%;min-height:32px;display:flex;flex-direction:column;align-items:flex-start;gap:12px}.typing-text{display:block;line-height:1.6;color:#e5e7eb;white-space:pre-wrap;word-break:break-word}`;
function MessageList() { const messages = useChatStore((s) => s.messages); const ref = useRef<HTMLDivElement>(null); const initialLast = useRef<string | null | undefined>(undefined); const lastScrolled = useRef<string | null>(null); useEffect(() => { const c = ref.current; if (c) c.scrollTop = c.scrollHeight; }, []); useEffect(() => { const c = ref.current; if (!c) return; const container = c.firstElementChild as HTMLElement; if (!container) return; const lastUser = [...messages].reverse().find((m) => m.role === "user"); if (initialLast.current === undefined) { initialLast.current = lastUser ? lastUser.id : null; return; } if (!lastUser) return; if (lastScrolled.current === lastUser.id) return; lastScrolled.current = lastUser.id; requestAnimationFrame(() => { const el = container.querySelector(`[data-mid="${lastUser.id}"]`) as HTMLElement; if (!el) return; const lastChild = container.lastElementChild as HTMLElement; const contentH = lastChild.offsetTop + lastChild.offsetHeight; const elTop = el.offsetTop; const need = elTop - 8 + c.clientHeight - contentH; const cur = parseFloat(getComputedStyle(container).paddingBottom) || 0; if (need > cur) container.style.paddingBottom = `${need}px`; requestAnimationFrame(() => { el.scrollIntoView({ block: "start", behavior: "smooth" }); }); setTimeout(() => el.scrollIntoView({ block: "start", behavior: "smooth" }), 80); }); }, [messages]); return (<><style>{mlCSS}</style><div className="message-scroll" ref={ref}><div className="message-container">{messages.map((m) => m.role === "user" ? (<UserMessage key={m.id} mid={m.id} content={m.content} attachments={m.attachments} />) : (<AiMessage key={m.id} mid={m.id} message={m as any} />))}</div></div></>); }

export interface PendingAttachment { id: string; name: string; kind: "image" | "pdf" | "text"; mimeType: string; base64: string; text?: string; previewUrl?: string }
function readFileAsAttachment(file: File): Promise<PendingAttachment | null> { return new Promise((resolve) => { if (file.size > 4 * 1024 * 1024) return resolve(null); const isImage = file.type.startsWith("image/"); const isPdf = file.type === "application/pdf"; const isText = file.type.startsWith("text/") || /\.(md|txt|json|js|ts|tsx|jsx|html|css|csv)$/i.test(file.name); const reader = new FileReader(); if (isImage || isPdf) { reader.onload = () => { const result = String(reader.result || ""); resolve({ id: rid(), name: file.name, kind: isImage ? "image" : "pdf", mimeType: file.type, base64: result.split(",")[1] || "", previewUrl: isImage ? result : undefined }); }; reader.onerror = () => resolve(null); reader.readAsDataURL(file); } else if (isText) { reader.onload = () => resolve({ id: rid(), name: file.name, kind: "text", mimeType: file.type || "text/plain", base64: "", text: String(reader.result || "") }); reader.onerror = () => resolve(null); reader.readAsText(file); } else resolve(null); }); }

function safeCopy(t: string) { try { if (navigator.clipboard?.writeText) { navigator.clipboard.writeText(t).catch(() => {}); return; } const ta = document.createElement("textarea"); ta.value = t; ta.style.position = "fixed"; ta.style.opacity = "0"; document.body.appendChild(ta); ta.select(); try { (document as any).execCommand("copy"); } catch {} document.body.removeChild(ta); } catch {} }

const ibCSS = `.input-wrapper{position:absolute;left:0;right:0;bottom:0;padding:8px 16px 14px 16px;background:linear-gradient(to top,#000 60%,transparent);z-index:10;width:100%;transition:bottom .05s linear}.input-bar{background:rgba(255,255,255,.045);backdrop-filter:blur(40px) saturate(200%) brightness(1.1);-webkit-backdrop-filter:blur(40px) saturate(200%) brightness(1.1);border:1px solid rgba(255,255,255,.12);box-shadow:0 8px 32px rgba(0,0,0,.45),0 1px 0 rgba(255,255,255,.08) inset;border-radius:16px;width:100%;max-width:650px;margin:0 auto;padding:14px 18px 12px 18px;display:flex;flex-direction:column;position:relative}.attach-row{display:flex;flex-wrap:wrap;gap:8px;margin-bottom:8px;position:relative;z-index:1}.attach-chip{display:flex;align-items:center;gap:8px;background:rgba(255,255,255,.07);border:1px solid rgba(255,255,255,.12);border-radius:10px;padding:6px 10px;font-size:12px;color:rgba(255,255,255,.75);max-width:200px}.attach-chip span{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.attach-thumb{width:34px;height:34px;border-radius:8px;object-fit:cover;border:1px solid rgba(255,255,255,.15)}.attach-remove{background:none;border:none;color:rgba(255,255,255,.5);cursor:pointer;font-size:14px;padding:0 2px}textarea{background:transparent;border:none;outline:none;color:#fff;-webkit-text-fill-color:#fff;caret-color:#fff;font-size:16px;font-family:inherit;width:100%;resize:none;min-height:40px;max-height:250px;line-height:1.5;margin-bottom:6px;position:relative;z-index:1}textarea::placeholder{color:rgba(255,255,255,.35);-webkit-text-fill-color:rgba(255,255,255,.35)}.action-row{display:flex;justify-content:space-between;align-items:center;position:relative;z-index:1}.action-left{display:flex;align-items:center;gap:0}.action-right{display:flex;align-items:center;gap:8px}.plus-btn{background:transparent;border:none;outline:none;color:rgba(255,255,255,.85);width:36px;height:36px;display:flex;align-items:center;justify-content:center;cursor:pointer;border-radius:50%;margin-left:-8px}.plus-btn.spin-cw svg{animation:spinCW .75s cubic-bezier(.25,0,.2,1) forwards}.plus-btn.spin-ccw svg{animation:spinCCW .75s cubic-bezier(.25,0,.2,1) forwards}@keyframes spinCW{from{transform:rotate(0)}to{transform:rotate(720deg)}}@keyframes spinCCW{from{transform:rotate(0)}to{transform:rotate(-720deg)}}.model-btn{display:flex;align-items:center;gap:6px;background:transparent;border:none;border-radius:20px;color:rgba(255,255,255,.85);font-size:14px;font-weight:500;font-family:inherit;padding:6px 8px;cursor:pointer;white-space:nowrap}.model-btn .mchev{display:inline-flex;transition:transform .3s ease;color:rgba(255,255,255,.85)}.model-btn.open .mchev{transform:rotate(180deg)}.model-btn .mchev svg{stroke:currentColor}.pop-menu{position:absolute;bottom:calc(100% + 10px);left:0;background:rgba(18,18,22,.96);border:1px solid rgba(255,255,255,.12);border-radius:14px;width:240px;padding:6px 4px;display:flex;flex-direction:column;gap:4px;backdrop-filter:blur(40px);box-shadow:0 16px 40px rgba(0,0,0,.6);z-index:60;opacity:0;pointer-events:none;transform:translateY(8px) scale(.96);transform-origin:bottom left;transition:opacity .22s,transform .22s}.pop-menu.show{opacity:1;pointer-events:all;transform:translateY(0) scale(1)}.model-item{padding:10px;border-radius:10px;cursor:pointer;display:flex;align-items:center;gap:8px}.model-item:hover{background:rgba(255,255,255,.04)}.model-item.locked{opacity:.55}.model-item-content{flex:1;display:flex;flex-direction:column;gap:2px}.model-title{font-size:13.5px;font-weight:500;color:#fff;display:flex;align-items:center;gap:6px}.model-desc{font-size:11px;color:#9ba1a6}.model-check{width:15px;height:15px;stroke:#fff;stroke-width:2.2;flex-shrink:0}.model-lock{width:12px;height:12px;stroke:#ff8080;stroke-width:2;flex-shrink:0}.beta-tag{color:#9ba1a6;margin-left:3px}.upload-opt{display:flex;align-items:center;gap:12px;padding:13px 16px;cursor:pointer;font-size:14px;color:rgba(255,255,255,.8)}.upload-opt:hover{background:rgba(255,255,255,.08)}.upload-opt.on{color:#7ab8ff}.upload-opt.on svg{stroke:#7ab8ff}.cv-cross{margin-left:auto;width:22px;height:22px;border-radius:7px;display:flex;align-items:center;justify-content:center;color:#7ab8ff;font-size:15px}.cv-pill{display:flex;align-items:center;gap:6px;padding:8px 12px;border-radius:18px;border:1px solid rgba(122,184,255,.4);background:rgba(122,184,255,.08);color:#7ab8ff;font-size:12px;font-family:'DM Sans',sans-serif;cursor:pointer}.cv-pill svg{stroke:currentColor}.send-btn{background:rgba(255,255,255,.12);border:1px solid rgba(255,255,255,.1);outline:none;width:36px;height:36px;border-radius:50%;display:flex;align-items:center;justify-content:center;cursor:pointer;transition:transform .15s ease